overlay: 1.0.0
info:
  title: Overlay mixpeek.yml => mixpeek_with_security.yml
  version: 0.0.0
actions:
  - target: $
    update:
      security:
        - bearerAuth: []
  - target: $["components"]
    update:
      securitySchemes:
        bearerAuth:
          type: http
          scheme: bearer
  - target: $["components"]["schemas"]
    remove: true

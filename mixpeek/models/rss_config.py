# coding: utf-8

"""
    Mixpeek API

    This is the Mixpeek API, providing access to various endpoints for data processing and retrieval.

    The version of the OpenAPI document: 0.81.1
    Contact: info@mixpeek.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from mixpeek.models.rss_http_header_credentials import RSSHttpHeaderCredentials
from typing import Optional, Set
from typing_extensions import Self

class RSSConfig(BaseModel):
    """
    RSS/Atom feed configuration. source_path = feed URL.
    """ # noqa: E501
    provider_type: Optional[StrictStr] = 'rss'
    credentials: Optional[RSSHttpHeaderCredentials] = Field(default=None, description="Optional HTTP headers for private feeds.")
    user_agent: Optional[StrictStr] = Field(default='Mixpeek RSS Sync/1.0', description="User-Agent header for feed requests.")
    request_timeout: Optional[Annotated[int, Field(le=120, strict=True, ge=5)]] = Field(default=30, description="HTTP request timeout in seconds.")
    __properties: ClassVar[List[str]] = ["provider_type", "credentials", "user_agent", "request_timeout"]

    @field_validator('provider_type')
    def provider_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['rss']):
            raise ValueError("must be one of enum values ('rss')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RSSConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of credentials
        if self.credentials:
            _dict['credentials'] = self.credentials.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RSSConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "provider_type": obj.get("provider_type") if obj.get("provider_type") is not None else 'rss',
            "credentials": RSSHttpHeaderCredentials.from_dict(obj["credentials"]) if obj.get("credentials") is not None else None,
            "user_agent": obj.get("user_agent") if obj.get("user_agent") is not None else 'Mixpeek RSS Sync/1.0',
            "request_timeout": obj.get("request_timeout") if obj.get("request_timeout") is not None else 30
        })
        return _obj



# coding: utf-8

"""
    Mixpeek API

    This is the Mixpeek API, providing access to various endpoints for data processing and retrieval.

    The version of the OpenAPI document: 0.81
    Contact: info@mixpeek.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from mixpeek.models.no_reduction import NoReduction
from mixpeek.models.tsne_params import TSNEParams
from mixpeek.models.umap_params import UMAPParams
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

DIMENSIONALITYREDUCTION_ONE_OF_SCHEMAS = ["NoReduction", "TSNEParams", "UMAPParams"]

class DimensionalityReduction(BaseModel):
    """
    Optional dimensionality reduction prior to clustering
    """
    # data type: TSNEParams
    oneof_schema_1_validator: Optional[TSNEParams] = None
    # data type: UMAPParams
    oneof_schema_2_validator: Optional[UMAPParams] = None
    # data type: NoReduction
    oneof_schema_3_validator: Optional[NoReduction] = None
    actual_instance: Optional[Union[NoReduction, TSNEParams, UMAPParams]] = None
    one_of_schemas: Set[str] = { "NoReduction", "TSNEParams", "UMAPParams" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = DimensionalityReduction.model_construct()
        error_messages = []
        match = 0
        # validate data type: TSNEParams
        if not isinstance(v, TSNEParams):
            error_messages.append(f"Error! Input type `{type(v)}` is not `TSNEParams`")
        else:
            match += 1
        # validate data type: UMAPParams
        if not isinstance(v, UMAPParams):
            error_messages.append(f"Error! Input type `{type(v)}` is not `UMAPParams`")
        else:
            match += 1
        # validate data type: NoReduction
        if not isinstance(v, NoReduction):
            error_messages.append(f"Error! Input type `{type(v)}` is not `NoReduction`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in DimensionalityReduction with oneOf schemas: NoReduction, TSNEParams, UMAPParams. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in DimensionalityReduction with oneOf schemas: NoReduction, TSNEParams, UMAPParams. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into TSNEParams
        try:
            instance.actual_instance = TSNEParams.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into UMAPParams
        try:
            instance.actual_instance = UMAPParams.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into NoReduction
        try:
            instance.actual_instance = NoReduction.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into DimensionalityReduction with oneOf schemas: NoReduction, TSNEParams, UMAPParams. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into DimensionalityReduction with oneOf schemas: NoReduction, TSNEParams, UMAPParams. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], NoReduction, TSNEParams, UMAPParams]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())



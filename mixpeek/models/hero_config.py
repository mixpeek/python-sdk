# coding: utf-8

"""
    Mixpeek API

    This is the Mixpeek API, providing access to various endpoints for data processing and retrieval.

    The version of the OpenAPI document: 0.81.1
    Contact: info@mixpeek.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class HeroConfig(BaseModel):
    """
    Hero section configuration for a page.
    """ # noqa: E501
    headline: StrictStr = Field(description="Main headline text")
    subheadline: Optional[StrictStr] = Field(default=None, description="Optional subheadline text")
    background_type: Optional[StrictStr] = Field(default='solid', description="Background type")
    background_url: Optional[StrictStr] = Field(default=None, description="URL for image or video background")
    background_color: Optional[StrictStr] = Field(default=None, description="Background color for solid/gradient")
    text_color: Optional[StrictStr] = Field(default='#FFFFFF', description="Hero text color")
    logo_url: Optional[StrictStr] = Field(default=None, description="Logo URL displayed in hero")
    height: Optional[StrictStr] = Field(default='420px', description="Hero section height")
    cta_label: Optional[StrictStr] = Field(default=None, description="Call-to-action button label")
    cta_url: Optional[StrictStr] = Field(default=None, description="Call-to-action button URL")
    __properties: ClassVar[List[str]] = ["headline", "subheadline", "background_type", "background_url", "background_color", "text_color", "logo_url", "height", "cta_label", "cta_url"]

    @field_validator('background_type')
    def background_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['image', 'video', 'gradient', 'solid']):
            raise ValueError("must be one of enum values ('image', 'video', 'gradient', 'solid')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of HeroConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of HeroConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "headline": obj.get("headline"),
            "subheadline": obj.get("subheadline"),
            "background_type": obj.get("background_type") if obj.get("background_type") is not None else 'solid',
            "background_url": obj.get("background_url"),
            "background_color": obj.get("background_color"),
            "text_color": obj.get("text_color") if obj.get("text_color") is not None else '#FFFFFF',
            "logo_url": obj.get("logo_url"),
            "height": obj.get("height") if obj.get("height") is not None else '420px',
            "cta_label": obj.get("cta_label"),
            "cta_url": obj.get("cta_url")
        })
        return _obj



# coding: utf-8

"""
    Mixpeek API

    This is the Mixpeek API, providing access to various endpoints for data processing and retrieval.

    The version of the OpenAPI document: 0.81.1
    Contact: info@mixpeek.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from mixpeek.models.instagram_o_auth_credentials import InstagramOAuthCredentials
from typing import Optional, Set
from typing_extensions import Self

class InstagramConfig(BaseModel):
    """
    Instagram Business/Creator account configuration via Meta Graph API.  Enables Mixpeek to sync media (posts, reels, carousels) from Instagram accounts using the official Instagram Graph API. Each media item becomes a bucket object with metadata (caption, likes, comments, permalink).  Authentication:     - OAuth 2.0 via Meta Developer Console     - Long-lived tokens (60 days) with automatic refresh  Requirements:     - Instagram Business or Creator account     - Facebook Page linked to the Instagram account     - Meta app with instagram_basic permission  Use Cases:     - Sync creator content for talent analysis     - Monitor competitor ad creatives     - Build social media content libraries for multimodal search
    """ # noqa: E501
    provider_type: Optional[StrictStr] = 'instagram'
    credentials: InstagramOAuthCredentials = Field(description="OAuth credentials for Instagram Graph API access.")
    scopes: Optional[List[StrictStr]] = Field(default=None, description="OAuth scopes granted during authorization.")
    business_account_id: Optional[StrictStr] = Field(default=None, description="Instagram Business Account ID if different from the user ID. Auto-populated from OAuth flow.")
    __properties: ClassVar[List[str]] = ["provider_type", "credentials", "scopes", "business_account_id"]

    @field_validator('provider_type')
    def provider_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['instagram']):
            raise ValueError("must be one of enum values ('instagram')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of InstagramConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of credentials
        if self.credentials:
            _dict['credentials'] = self.credentials.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of InstagramConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "provider_type": obj.get("provider_type") if obj.get("provider_type") is not None else 'instagram',
            "credentials": InstagramOAuthCredentials.from_dict(obj["credentials"]) if obj.get("credentials") is not None else None,
            "scopes": obj.get("scopes"),
            "business_account_id": obj.get("business_account_id")
        })
        return _obj



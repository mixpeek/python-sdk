# coding: utf-8

"""
    Mixpeek API

    This is the Mixpeek API, providing access to various endpoints for data processing and retrieval.

    The version of the OpenAPI document: 0.81
    Contact: info@mixpeek.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class BlobURLRef(BaseModel):
    """
    Reference to a document-related blob (thumbnails, sources, artifacts).  DevEx guidelines: - field: Use a stable, semantic label such as \"thumbnail\", \"source\", or a domain term.   Avoid leaking internal paths like \"metadata.thumbnail[0]\"; indexing (e.g., per-chunk)   should be inferred from filenames or stored separately (e.g., ordinal) if needed. - role: Determines behavior/UX grouping (thumbnail|source|artifact|aux). Prefer using role for UI. - url: Canonical locator. s3:// URLs are presignable via API when return_url=true. - presigned_url: Response-only, set by API to a time-limited https link.
    """ # noqa: E501
    var_field: StrictStr = Field(description="Stable semantic label for the blob (e.g., 'thumbnail', 'source').", alias="field")
    role: Optional[StrictStr] = Field(default='source', description="Semantic role of this blob")
    type: Optional[StrictStr] = Field(default='other', description="Schema/blob type")
    url: StrictStr = Field(description="Canonical blob URL; s3:// URLs are presignable")
    object_key: Optional[StrictStr] = Field(default=None, description="S3 object key if available (no bucket prefix)")
    filename: Optional[StrictStr] = Field(default=None, description="Leaf filename")
    size_bytes: Optional[StrictInt] = Field(default=None, description="Blob size in bytes")
    content_type: Optional[StrictStr] = Field(default=None, description="MIME content type")
    checksum: Optional[StrictStr] = Field(default=None, description="Optional checksum")
    created_at: Optional[datetime] = Field(default=None, description="Blob create time")
    source_blob_id: Optional[StrictStr] = Field(default=None, description="Cross-reference to source blob in Mongo lineage")
    presigned_url: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(default=None, description="Response-only presigned https URL")
    __properties: ClassVar[List[str]] = ["field", "role", "type", "url", "object_key", "filename", "size_bytes", "content_type", "checksum", "created_at", "source_blob_id", "presigned_url"]

    @field_validator('role')
    def role_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['source', 'thumbnail', 'artifact', 'aux']):
            raise ValueError("must be one of enum values ('source', 'thumbnail', 'artifact', 'aux')")
        return value

    @field_validator('type')
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['video', 'image', 'audio', 'text', 'pdf', 'other']):
            raise ValueError("must be one of enum values ('video', 'image', 'audio', 'text', 'pdf', 'other')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BlobURLRef from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BlobURLRef from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "field": obj.get("field"),
            "role": obj.get("role") if obj.get("role") is not None else 'source',
            "type": obj.get("type") if obj.get("type") is not None else 'other',
            "url": obj.get("url"),
            "object_key": obj.get("object_key"),
            "filename": obj.get("filename"),
            "size_bytes": obj.get("size_bytes"),
            "content_type": obj.get("content_type"),
            "checksum": obj.get("checksum"),
            "created_at": obj.get("created_at"),
            "source_blob_id": obj.get("source_blob_id"),
            "presigned_url": obj.get("presigned_url")
        })
        return _obj



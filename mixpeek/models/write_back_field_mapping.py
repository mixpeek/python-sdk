# coding: utf-8

"""
    Mixpeek API

    This is the Mixpeek API, providing access to various endpoints for data processing and retrieval.

    The version of the OpenAPI document: 0.81.1
    Contact: info@mixpeek.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class WriteBackFieldMapping(BaseModel):
    """
    Maps a field from retriever results back to the document.  Controls how retriever result fields are written to the source document.  Attributes:     source_field: Field path in retriever result (dot notation for nested fields)     target_field: Field name to write on the document     mode: How to aggregate values across multiple results:         - \"first\": Write value from first result only (default)         - \"all_as_array\": Collect values from all results into a list         - \"concat\": Concatenate string values with \", \" separator
    """ # noqa: E501
    source_field: StrictStr = Field(description="Field path in retriever result (dot notation supported)")
    target_field: StrictStr = Field(description="Field name to write on the document")
    mode: Optional[StrictStr] = Field(default='first', description="How to aggregate values from multiple results. 'first': top result only. 'all_as_array': collect into list. 'concat': join strings with ', '.")
    __properties: ClassVar[List[str]] = ["source_field", "target_field", "mode"]

    @field_validator('mode')
    def mode_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['first', 'all_as_array', 'concat']):
            raise ValueError("must be one of enum values ('first', 'all_as_array', 'concat')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of WriteBackFieldMapping from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of WriteBackFieldMapping from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "source_field": obj.get("source_field"),
            "target_field": obj.get("target_field"),
            "mode": obj.get("mode") if obj.get("mode") is not None else 'first'
        })
        return _obj



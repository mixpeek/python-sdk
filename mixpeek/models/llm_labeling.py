# coding: utf-8

"""
    Mixpeek API

    This is the Mixpeek API, providing access to various endpoints for data processing and retrieval.

    The version of the OpenAPI document: 0.81
    Contact: info@mixpeek.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class LLMLabeling(BaseModel):
    """
    Configuration for LLM-based cluster labeling.
    """ # noqa: E501
    enabled: Optional[StrictBool] = Field(default=False, description="Whether to generate labels for clusters using LLM")
    provider: Optional[StrictStr] = Field(default=None, description="LLM provider to use for labeling. Supported: 'openai', 'google'")
    model_name: Optional[StrictStr] = Field(default=None, description="LLM model identifier. For OpenAI defaults to 'gpt-4o-mini'. For Google defaults to 'gemini-2.5-flash'.")
    include_summary: Optional[StrictBool] = Field(default=True, description="Whether to generate cluster summaries")
    include_keywords: Optional[StrictBool] = Field(default=True, description="Whether to extract keywords for clusters")
    max_samples_per_cluster: Optional[StrictInt] = Field(default=10, description="Maximum samples to send to LLM per cluster")
    custom_prompt: Optional[StrictStr] = Field(default=None, description="Custom prompt template for labeling")
    parameters: Optional[Dict[str, Any]] = Field(default=None, description="Provider-specific parameters forwarded to the LLM service. For OpenAI: temperature, max_tokens, top_p, json_output, etc. For Google: temperature, top_k, max_output_tokens, json_output, etc.")
    __properties: ClassVar[List[str]] = ["enabled", "provider", "model_name", "include_summary", "include_keywords", "max_samples_per_cluster", "custom_prompt", "parameters"]

    @field_validator('provider')
    def provider_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['openai', 'google']):
            raise ValueError("must be one of enum values ('openai', 'google')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of LLMLabeling from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of LLMLabeling from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "enabled": obj.get("enabled") if obj.get("enabled") is not None else False,
            "provider": obj.get("provider"),
            "model_name": obj.get("model_name"),
            "include_summary": obj.get("include_summary") if obj.get("include_summary") is not None else True,
            "include_keywords": obj.get("include_keywords") if obj.get("include_keywords") is not None else True,
            "max_samples_per_cluster": obj.get("max_samples_per_cluster") if obj.get("max_samples_per_cluster") is not None else 10,
            "custom_prompt": obj.get("custom_prompt"),
            "parameters": obj.get("parameters")
        })
        return _obj


